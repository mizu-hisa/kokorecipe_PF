  <div>
    <h4 class="mt-5 text-center"><%= @customer.customer_name %>さんの会員詳細</h4>
  </div>

<div class='container px-10 offset-4'>
  <table class="table table-borderless">
  
      <div class="mt-5"></div>
  
        <tr>
          <td width="30%">ID</td>
          <td><%= @customer.id %></td>
        </tr>
  
        <tr>
          <td>Name</td>
          <td><%= @customer.customer_name %></td>
        </tr>
  
        <tr>
          <td>Status</td>
          <td><% if @customer.is_deleted == true %><p class="text-secondary"><strong>退会済み</strong></p>
              <%else%> <p class="text-success"> <strong>有効</strong></p>
              <%end%>
          </td>
        </tr>
  </table>
</div>

      <div class="col offset-4 mt-3 mb-5">
        <%=link_to "編集する", edit_admin_customer_path(@customer), class: 'btn btn-success ' %>
        <%=link_to "ユーザ情報一覧へ戻る", admin_customers_path(@customers), class: 'btn btn-primary ml-5' %>
      </div>
